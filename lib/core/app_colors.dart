import 'package:flutter/material.dart';

/// Global color palette for the portfolio app.
///
/// All widgets should reference these values (directly or via the theme) so
/// that the entire UI can be switched/updated by changing this single place.
class AppColors {
  static const Color backgroundPrimary = Color(0xFF141E27);
  static const Color backgroundSecondary = Color(0xFF19232C);

  static const Color accentColor = Color(0xFFEE7A62);
  static const Color accentHover = Color(0xFFF37C64);

  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFAAB2B8);
}